package WishLIst;
import Menu.*;
import InternalOptions.*;
public class WishlistAvaliable {
	public static void display() {
		if() {//nothing inside the wishlist 
			System.out.println("[No items in Wish List]");
			Menu.display();
		}
		else {
			// selected wishlist should be displayed;
			InternalOptions.options();
		}
	}
}
